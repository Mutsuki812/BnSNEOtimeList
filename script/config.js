/* ============================================================
   設定 & 常數
   集中管理所有全域設定、靜態資料與文字內容
   ============================================================ */

// ============================================================
// 應用程式核心設定
// ============================================================

export const CONFIG = {
  // Supabase 連線設定
  SUPABASE_CDN: "https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/dist/umd/supabase.min.js", // SDK CDN 來源 (UMD 格式)
  SUPABASE_URL: "https://bagzrimxitmegqpmvnfi.supabase.co",                                       // Supabase 專案網址
  SUPABASE_KEY: "sb_publishable_IBRmc1srGQDHE37cn_f-ZQ_-vzy-NdW",                                // Supabase Publishable Key (可公開)

  // 計時器間隔 (單位：毫秒)
  REFRESH_INTERVAL: 60_000,   // 每分鐘重新渲染一次任務列表
  HOUR_INTERVAL:  3_600_000,  // 每小時觸發一次完整的初始化流程
};

// ============================================================
// 任務類型定義
// 每個物件對應一種世界王/活動類型
// ============================================================

export const TASK_TYPES = [
  {
    key: "gishiki",          // 程式內部識別鍵
    label: "可疑的儀式",      // 顯示名稱
    color: "#7a4171",        // 主題色 (莫蘭迪紫)
    offsetMin: 10,           // 出字後 Boss 登場的延遲分鐘數
    useOnlineSystem: true,   // 是否啟用線上即時回報系統
  },
  {
    key: "shirao",
    label: "白青野王",
    color: "#7b8d42",        // 主題色 (莫蘭迪綠)
    offsetMin: 5,
    useOnlineSystem: true,
  },
  {
    key: "sengen",
    label: "仙幻島野王",
    color: "#B08F3E",        // 主題色 (莫蘭迪金)
    offsetMin: 5,
    useOnlineSystem: true,
  },
];

// ============================================================
// 回報表單選項
// ============================================================

/** 回報表單的任務選項 (顯示給使用者選擇) */
export const REPORT_TASK = ["可疑的儀式", "白青野王", "仙幻島野王", "其他"];

/** 回報類型選項：依任務類型分兩組 */
export const REPORT_TYPES = {
  default:   ["數據提供", "時間修正", "地點修正"], // 適用於三種野王/儀式
  otherOnly: ["音效相關", "BUG回報", "想説"],       // 僅適用於「其他」類型
};

// ============================================================
// 正則表達式
// ============================================================

/** 用於識別維護時段的任務內容 */
export const MAINTENANCE_PATTERN = /例行維護中/;

// ============================================================
// 靜態資料
// ============================================================

/** 星期對應文字陣列，索引對應 Date.getDay() 的回傳值 (0=日 ~ 6=六) */
export const WEEKDAYS = ["日", "一", "二", "三", "四", "五", "六"];

// ============================================================
// 顯示文字內容
// 集中管理所有對使用者顯示的靜態文字
// ============================================================

export const TEXTS = {
  /** 頂部標題公告 (固定顯示) */
  titleNotice:
    "<b>仙幻島第６賽季　2026.06.24 - 2026.07.29</b><br>",

  /** 常態說明文字 (活動期間外顯示) */
  regularNotice:
    "・表記時間 = 系統出字時間<br>" +
    "・儀式：出字提示後 3分鐘Boss登場、沒人打30分鐘後消失。<br>" +
    "・白青/仙幻島野王：出字提示後5分鐘Boss登場。<br>",

  /** 限時說明文字 (活動期間內顯示) */
  temporaryNotice:
    "・該系統極致仰賴各位回報者的準確度 如有錯誤的回報 請記得自行刪除<br>" +
    "・回報或留言需登入名稱（點擊回報左側的時鐘 可以查看或刪除歷史紀錄）<br>",

  /**
   * 前一小時提示文字
   * 當前一小時的任務可能尚未消失時，顯示給玩家的行動提示
   */
  previousHourHint: {
    gishiki: "可能還沒死，可以找找看",
    shirao:  "可能還沒死，可以找找看",
    sengen:  "跑圖快的，還不快飛",
  },

  /** 回報區塊的常態說明 (非活動期間顯示) */
  Report_regularNotice: "野王重置 當週請使用上方↑↑↑↑↑ 回報<br>",

  /** 回報區塊的留言引導文字 */
  Report_temporaryNotice: "有想説的 可以留言在這裡↓↓↓↓↓ 感謝",
};

